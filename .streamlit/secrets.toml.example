# Streamlit Cloud secrets — copy to secrets.toml (local) or paste into
# the Streamlit Cloud dashboard Settings → Secrets.
#
# All keys must be prefixed with FT13F_ to match pydantic-settings.
# .streamlit/secrets.toml is git-ignored and never committed.

# Required: SEC EDGAR needs a User-Agent with your name + email
FT13F_EDGAR_USER_AGENT = "YourName your@email.com"

# Optional: OpenFIGI API key for CUSIP → ticker resolution
# Free key at https://www.openfigi.com/api — raises rate limit from 5 to 100 req/min
# FT13F_OPENFIGI_API_KEY = "your-api-key-here"
